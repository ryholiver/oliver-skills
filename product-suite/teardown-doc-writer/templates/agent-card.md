# Agent 卡片模板

使用说明：技术层每个核心Agent单独一张卡片，按此格式填写。

---

## Agent：{AGENT_NAME}

> 推导版 | 置信度：{高/中/低}

**一句话定义**：{这个Agent负责什么，为什么需要它单独存在}

**在架构中的位置**：{处于哪个层（输入处理/规划/执行/质检/交互），与哪些Agent相邻}

**触发时机**：{在什么条件下被调用，由谁触发}

---

### 输入

| 字段 | 类型 | 来源 | 推断依据 |
|------|------|------|---------|
| {字段名} | {string/list/etc} | {全局上下文/前一Agent/用户输入} | {[体验推导]/[合理推测]} |
| ... | | | |

---

### 处理逻辑

**范式**：{Plan-and-Execute / ReAct / 纯Workflow}

**判断依据**：{为什么判断为这个范式}

**步骤推导**：
1. {第一步：做什么，为什么这么做}
2. {第二步}
3. {第三步}
4. ...

---

### 工具集

| 工具 | 类型 | 用途 | 推断依据 |
|------|------|------|---------|
| {工具名} | {搜索/代码执行/图像处理/数据库/渲染/...} | {具体用途} | {推断依据} |
| ... | | | |

---

### 输出

| 字段 | 类型 | 写入到 | 说明 |
|------|------|--------|------|
| {字段名} | {类型} | {全局上下文字段} | {说明} |
| ... | | | |

---

### 规则与边界

**必须遵守**：
- {约束1}
- {约束2}

**被禁止的行为**：
- {约束}

**条件逻辑**（如有）：
- 如果 {条件} 则 {行为}
- 如果 {条件} 则 {回退策略}

---

### 推测的 Prompt 骨架

> ⚠️ 仅在"复刻参考"用途时填写此部分。以下为推导版，非实际Prompt。

```
角色定义：
你是一个{角色}，负责{职责}。

输入：
- {字段}: {说明}

任务步骤：
1. {步骤}
2. {步骤}

输出格式：
{格式说明}

约束：
- {约束}
```

---

### 已知问题（如有）

| 问题 | 现象 | 可能根因 | 改进方向 |
|------|------|---------|---------|
| {问题描述} | {观察到的表现} | {Prompt/工具/流程/模型} | {建议} |

---

*置信度：{高/中/低} | 主要不确定点：{说明}*
